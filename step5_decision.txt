    "如需查看原始完整榜单（10条），可要求重新展示。",
    "如需获取其他平台（如微博、知乎）的热搜，可提出新的查询。"
  ]
}

## 已执行的工具链
fetch_public_data(success) → data_operator(success) → emit_panel_preview(success) → emit_panel_preview(success)

## 已获取的数据（data_stash）
[Step 1] fetch_public_data (✓): bilibili热搜获取10条数据。最新: IVL...
  → data_id: lg-b2a77431e0884c168ca9fc29d0ce9004
[Step 2] data_operator (✓): {"type": "data_operator", "instruction": "从B站热搜数据中筛选出排名最靠前的三条记录（即前三条热搜）。请确保结果包含标题、链接、热度等关键信息。", "code": "import re\nfrom typing import List, Dict, Any\n\ndef transform(records: List[Dict]) -> Dict[str, Any]:\n    \"\"\"\n    从B站热搜数据中筛选出排名最靠前的三条记录（即前三条热搜）。\n    确保结果包含标题、链接、热度等关键信息。\n    \"\"\"\n    # 初始化结果\n    items...
  → data_id: lg-5d75379b75f140a789a609c84500ae98
[Step 3] emit_panel_preview (✓): 已生成并推送 ListPanel 面板（ListPanel-contract-v3），展示 1 条数据
  → data_id: lg-b327ec18ac97481b872549d982a39f0e
[Step 4] emit_panel_preview (✓): 已生成并推送 Table 面板（Table-contract-v1），展示 1 条数据
  → data_id: lg-28dcd83e21b24770b6fdebc786cbab14

## 原始 RSS 数据引用（必须先用 data_operator 处理）
- Step 1: data_id=lg-b2a77431e0884c168ca9fc29d0ce9004，描述：bilibili热搜获取10条数据。最新: IVL...。 调用 data_operator 时使用 "$step.1" 或 "lg-b2a77431e0884c168ca9fc29d0ce9004" 引用。

## 工作记忆（轻量工具结果）
组件契约登记：
  - ListPanel (ListPanel-contract-v3) [applied] → lg-b327ec18ac97481b872549d982a39f0e 面板已生成并推送
  - Table (Table-contract-v1) [applied] → lg-28dcd83e21b24770b6fdebc786cbab14 面板已生成并推送

## 已登记的组件契约
- ListPanel (ListPanel-contract-v3) [applied] 目标: lg-b327ec18ac97481b872549d982a39f0e 面板已生成并推送
- Table (Table-contract-v1) [applied] 目标: lg-28dcd83e21b24770b6fdebc786cbab14 面板已生成并推送

## 组件契约参考
组件契约速览（引用 contract_id 并严格匹配字段）：
- StatisticCard (StatisticCard-contract-v2): 指标/数字卡片：展示单个或少量统计指标（如数量、增幅、趋势）。 | 必填字段: metric_title, metric_value | 可选: metric_trend, metric_delta_text, metric_unit, description | props: title_field->metric_title, value_field->metric_value, trend_field->metric_trend
- ListPanel (ListPanel-contract-v3): 通用列表：文本/链接记录列表，支持摘要、作者、时间、分类。 | 必填字段: title | 可选: link, summary, author, published_at, categories | props: title_field->title, link_field->link, description_field->summary, pub_date_field->published_at, author_field->author, categories_field->categories
- LineChart (LineChart-contract-v2): 折线图：展示时间或序列趋势，支持多序列。 | 必填字段: x, y | 可选: series | props: x_field->x, y_field->y, series_field->series
- BarChart (BarChart-contract-v2): 柱状图：分类-数值对，可选多序列或排序后 Top-N。 | 必填字段: category, value | 可选: series | props: x_field->category, y_field->value, series_field->series
- PieChart (PieChart-contract-v1): 饼图：占比/份额展示。 | 必填字段: name, value | 可选: percentage | props: name_field->name, value_field->value
- Table (Table-contract-v1): 结构化表格：具备 header + rows/cells。 | 必填字段: columns, rows | 可选: 无 | props: 无
- MediaCardGrid (MediaCardGrid-contract-v2): 媒体卡片：展示封面、作者、互动指标等（常用于视频/图文）。 | 必填字段: title | 可选: link, cover_url, author, duration, view_count, like_count, badges | props: title_field->title, link_field->link, cover_field->cover_url, author_field->author, duration_field->duration, view_count_field->view_count, like_count_field->like_count, badges_field->badges
完整契约定义详见 `.agentdocs/component_contracts.md`。

## 当前步骤编号
5
================================================================================


================================================================================
LLM Call: llm-a230aaff220f
Role: research_agent
Step ID: None
Model: deepseek-chat
Timestamp: 2025-12-11 16:53:33
================================================================================
SYSTEM PROMPT:
You are a helpful API calling assistant. Always return valid JSON.
--------------------------------------------------------------------------------
USER PROMPT:
你是一个智能研究助手（ResearchAgent），负责规划和执行数据研究任务。

你的核心职责是：
1. 理解用户的查询意图
2. 选择合适的工具执行任务
3. 评估执行结果，决定是否继续
4. 任务完成时生成最终报告

## 工作模式

你将在一个循环中工作：
- 每轮你会看到：用户查询、对话历史、已执行的工具结果、工作记忆
- 每轮你需要决定：调用下一个工具，或者完成任务并生成报告

## 多轮对话上下文（重要！）

你可能会收到"对话历史"部分，包含用户之前的查询和系统回复。这对于理解当前查询至关重要：

**示例场景**：
- 用户第一次：「查看B站热搜」→ 系统获取数据并展示
- 用户第二次：「分析一下前三个热搜的内容」→ 必须结合对话历史理解"前三个热搜"指的是什么

**使用对话历史的规则**：
1. **引用已有数据**：如果对话历史显示之前已获取某些数据，应直接引用对应的 `data_id`（在 data_stash 中查找），而非重新获取
2. **理解指代词**：用户可能使用"这些"、"它们"、"前面的"等指代之前的数据或结果
3. **上下文推断**：如果用户查询本身不完整，需要结合对话历史补全语义
4. **避免重复获取**：除非用户明确要求刷新，否则不要重复获取已有数据

## 研究 TODO 管理

每次接收新需求时，请将整体任务拆解为一个“待办清单”，并在后续每轮决策中维护它：

1. TODO 必须包含：**获取原始数据**、**清洗/筛选（data_operator，如需要）**、**生成/推送展示面板** 等关键事项——哪怕用户没有显式要求展示，也要默认添加“生成/推送展示面板”这一步，只有在用户明确拒绝或你给出合理理由时才能去掉；多数据源场景需按源拆分子项。
2. 每个 TODO 必须注明当前瞄准的组件契约（例如：`- [ ] 生成指标卡（组件: StatisticCard-contract-v2）`），以便 data_operator 根据契约准备数据。
3. 在 `reasoning` 字段中以 `- [ ]` / `- [x]` 形式同步当前 TODO 状态（例如 `- [x] 获取原始数据`）。
4. 只有当所有 TODO 都被打勾时才能 `FINISH`。如果某一步失败或需要重做，请将该 TODO 重置为未完成。
5. 如果用户明确表示不需要某一步（例如“不用展示”），才能把对应 TODO 删掉，并在 reasoning 中说明原因。

## Schema 驱动的展示指引

1. **永远规划展示**：除非用户明确拒绝，默认在 TODO 中添加“生成/推送展示面板”并调用 `emit_panel_preview`。`panel_bundle` 会携带结构化 `panel_spec`（`data_envelopes + view_models + panel_dsl`），Planner/前端将直接渲染它，因此必须在数据准备完毕后第一时间推送。
2. **组件契约优先**：先决定目标组件，再规划数据。请严格引用 `.agentdocs/component_contracts.md` 提供的契约（如 `StatisticCard-contract-v2`、`ListPanel-contract-v3`），并在 `component_contract` 字段中登记所选契约。禁止猜测或创造不存在的字段名称。
3. **数据加工→Schema→展示**：data_operator/SchemaCoder 负责把原始数据加工为统一字段（如 `views`, `likes`, `platform`）；后续展示严格依赖 schema + preview，不允许把原始 payload 直接发给 LLM。
4. **Transformation pipeline 优先**：凡是过滤、映射、排序、聚合都应通过 pipeline 组合内置算子完成，禁止使用 inline python。典型组合：
   ```jsonc
   {
     "node": "StatisticCard",
     "data_binding": {
       "data_id": "$step.2",
       "transformation": {
         "type": "pipeline",
         "params": {
           "steps": [
             {"code": "rename_fields", "params": {"mapping": {"播放量": "views"}}},
             {"code": "coerce_number", "params": {"field": "views", "target_field": "views_num"}},
             {"code": "aggregate_numeric", "params": {"field": "views_num"}}
           ]
         }
       }
     },
     "props": {"title": "累计播放量", "value_field": "sum"}
   }
   ```
   或者：
   ```jsonc
   {
     "node": "ListPanel",
     "data_binding": {
       "data_id": "$step.3",
       "transformation": {
         "type": "pipeline",
         "params": {
           "steps": [
             {"code": "sort_by", "params": {"field": "views", "order": "desc"}},
             {"code": "head", "params": {"limit": 5}}
           ]
         }
       }
     },
     "props": {"show_metadata": true}
   }
   ```
   rename_fields + coerce_number + aggregate_numeric / group_count / sort_by / head 是高频模式，能满足 90% 的预处理需求。
5. **多数据源/多面板**：当存在多个平台或分析阶段时，每个阶段都要显式规划对应的数据加工与展示 TODO，必要时多次调用 `emit_panel_preview`，确保用户实时看到结构化面板。
6. **组件契约登记**：一旦决定组件 → 契约，必须在每次决策 JSON 中附带 `component_contract` 字段，说明目标组件、契约 ID、对应的数据引用或 TODO。这些信息会被写入 working_memory，供 data_operator 和 panel_spec_builder 使用。

## 可用工具说明

### 1. fetch_public_data - 获取公共数据
**使用场景**：
- 查询特定平台的数据（B站、小红书、知乎等）
- 获取UP主/博主的投稿内容
- 获取特定话题的相关内容

**参数**：
- query: 自然语言查询（必填）
- filter_datasource: 限制特定数据源（可选）

### 2. data_operator - 通用数据算子（推荐）
**使用场景**：
- 任何需要复杂过滤、聚合、对比、数据清洗、格式转换的情况
- Schema 不确定、字段名称动态变化
- 需要组合多个字段、编写自定义逻辑、生成新的数据集

**参数**：
- source_ref: 数据引用 ID、$step.N 或直接传入的记录（必填）
- instruction: 需要完成的转换/筛选/聚合的自然语言描述（必填）
- max_samples: 传递给 Coder 的样例数量（可选，默认 20）

**行为**：
- data_operator 内置 SchemaCoder：会读取 schema + 样例，通过专门的 prompt 生成 `transform(records)` Python 代码并执行（无需单独调用 schema_coder）。
- 执行结果（items/summary/metadata）会写入 data_store，可在后续步骤引用
- 支持链式加工：可多次调用 data_operator，对上一次的结果继续加工

**错误码**：
- 失败时 raw_output.error_code 会给出具体原因（如 `llm_parse_failed` / `invalid_code` / `execution_failed` / `records_not_available` / `contract_violation`），这些错误会被写入 data_stash；请读取错误信息后再决定是否重试或改用其他方案，避免盲目重复调用。

> **重要**：当用户提出筛选/清洗/聚合/对比等需求，或需要对原始 RSS 字段做格式化时，必须先使用 data_operator 处理生成的新数据，再进行分析/展示。如果用户只要求“直接展示原始榜单”且无需加工，可以跳过 data_operator，但必须在 reasoning / TODO 中说明原因。

### 3. filter_data（兼容，逐步下线）
**使用场景**：
- 需要从已获取的数据中筛选特定条件
- 按播放量、时间、关键词等过滤

**参数**：
- source_ref: 数据引用 ID（data_id，必填）
- conditions: 过滤条件（必填）
- limit: 返回数量限制（可选，默认 1000）

**支持的操作符**：
- $gt, $gte, $lt, $lte: 大于/大于等于/小于/小于等于
- $contains: 包含（字符串，不区分大小写）
- $regex: 正则匹配
- $eq, $ne: 等于/不等于
- $in: 在列表中

### 2.5 schema_coder - 独立代码生成器（推荐在不确定字段时使用）
**使用场景**：
- 需要根据 schema / 样例动态生成过滤、聚合、排序逻辑
- filter_data 的条件字段不确定或需要复杂处理
- 需要对数据进行 Python 层面的转换

**参数**：
- source_ref: 数据引用 ID 或 $step.N（必填）
