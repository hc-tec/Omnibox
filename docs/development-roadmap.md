# 项目开发路线图

> 生成时间：2025-11-09
> 目标：明确后端和前端的开发优先级，从核心能力完善进入到适配器与组件矩阵扩展阶段

---

## 1. 当前状态评估

### 1.1 后端核心流程 ✅ 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| DataExecutor | ✅ | RSSHub 数据获取、缓存 |
| DataQueryService | ✅ | RAG 检索 + 路径生成 + 数据获取 |
| IntentService | ✅ | 意图识别 |
| ChatService | ✅ | 对话服务、metadata 暴露 |
| PanelGenerator | ✅ | 面板生成、fallback 处理 |
| DataBlockBuilder | ✅ | 数据块构建 |
| ComponentPlanner | ✅ | 规则引擎组件规划 |
| LLMComponentPlanner | ✅ | LLM 组件规划（带缓存） |
| LayoutEngine | ✅ | 布局引擎 |
| RouteAdapterRegistry | ✅ | 适配器注册与查找 |
| 契约验证 | ✅ | view_models.py Pydantic 验证 |
| 警告机制 | ✅ | 默认适配器、fallback、skip 警告 |

**结论**：后端核心架构已完备，数据流从用户查询到面板渲染的完整链路已打通。

---

### 1.2 已实现的适配器

| 数据源 | 路由 | 支持组件 | 状态 |
|--------|------|----------|------|
| Hupu | `/hupu/bbs/*` | ListPanel | ✅ |
| GitHub | `/github/trending/*` | ListPanel + LineChart | ✅ |
| Bilibili Feed | `/bilibili/user/video`, `/bilibili/user/dynamic` | ListPanel | ✅ |
| Bilibili Followings | `/bilibili/user/followings/*` | ListPanel | ✅ |
| Bilibili Hot Search | `/bilibili/hot-search` | ListPanel | ✅ |
| Generic | `/*` (兜底) | ListPanel | ✅ |

**总计**：6 个适配器（其中 1 个为兜底适配器）

**覆盖率**：RSSHub 支持数百个路由，当前覆盖率 < 5%

---

### 1.3 已定义的组件契约

| 组件 | 后端契约 | 前端实现 | 用途 |
|------|----------|----------|------|
| ListPanel | ✅ | ✅ | 列表展示（文章、帖子、视频等） |
| StatisticCard | ✅ | ✅ | 指标卡片（数值、趋势） |
| LineChart | ✅ | ✅ | 折线图/面积图 |
| FallbackRichText | ✅ | ✅ | 兜底渲染（调试用） |
| NumberView | ✅ (deprecated) | ❌ | 已废弃 |
| Table | ✅ (未使用) | ❌ | 表格（定义了但未实现） |

**总计**：4 个可用组件

---

### 1.4 组件缺失分析 ⚠️ 严重不足

#### 1.4.1 常见可视化组件（缺失）

| 组件类型 | 用途 | 优先级 | 典型场景 |
|----------|------|--------|----------|
| **BarChart** | 柱状图 | 🔴 高 | 排行榜、对比数据 |
| **PieChart** | 饼图 | 🟡 中 | 占比、分布 |
| **Table** | 表格 | 🔴 高 | 结构化数据展示 |
| **ProgressBar** | 进度条 | 🟡 中 | 完成度、占用率 |
| **TagCloud** | 标签云 | 🟢 低 | 关键词、热词 |
| **Timeline** | 时间线 | 🟡 中 | 历史事件、更新记录 |
| **Heatmap** | 热力图 | 🟢 低 | 活跃度、分布密度 |
| **Map** | 地图 | 🟢 低 | 地理数据 |
| **RadarChart** | 雷达图 | 🟢 低 | 多维度对比 |
| **Gauge** | 仪表盘 | 🟢 低 | 百分比、评分 |

#### 1.4.2 富文本与媒体组件（缺失）

| 组件类型 | 用途 | 优先级 |
|----------|------|--------|
| **ImageGallery** | 图片画廊 | 🟡 中 |
| **VideoPlayer** | 视频播放器 | 🟢 低 |
| **MarkdownView** | Markdown 渲染 | 🟡 中 |
| **CodeBlock** | 代码高亮 | 🟢 低 |

#### 1.4.3 交互式组件（缺失）

| 组件类型 | 用途 | 优先级 |
|----------|------|--------|
| **Tabs** | 多标签切换 | 🟡 中 |
| **Accordion** | 折叠面板 | 🟢 低 |
| **Carousel** | 轮播图 | 🟢 低 |

---

## 2. 核心问题

### 2.1 组件不足导致的问题

1. **数据展示单一**：
   - 90% 的数据只能用 ListPanel 展示
   - 无法进行有效的数据对比（没有 BarChart）
   - 无法展示结构化数据（Table 未实现）

2. **适配器开发受限**：
   - GitHub Trending 想展示排行榜 → 只能用 ListPanel
   - B 站热搜想展示热度对比 → 没有 BarChart
   - 数据统计类路由 → 只能用 StatisticCard 堆砌

3. **用户体验差**：
   - 大量数据只能看到列表，无法直观感受趋势
   - 缺少视觉吸引力
   - 信息密度低

### 2.2 适配器覆盖不足

- RSSHub 支持的热门数据源：
  - Bilibili：30+ 路由（目前覆盖 3 个）
  - GitHub：10+ 路由（目前覆盖 1 个）
  - 微博、知乎、豆瓣、Reddit 等：完全未覆盖
  - RSS 订阅源：完全未覆盖

---

## 3. 开发优先级建议

### 阶段划分

```
┌─────────────────────────────────────────────────────────┐
│ 第一阶段：核心组件补齐（1-2周）                          │
│ 目标：补齐 3-5 个高优先级组件，满足 80% 的数据展示需求   │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│ 第二阶段：适配器矩阵扩展（2-4周）                       │
│ 目标：为热门数据源（B站、GitHub、微博等）编写 20-30 个   │
│       适配器，利用新组件丰富展示效果                     │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│ 第三阶段：体验优化与扩展（持续）                         │
│ 目标：添加交互式组件、富媒体组件、优化性能               │
└─────────────────────────────────────────────────────────┘
```

---

## 4. 第一阶段：核心组件补齐（优先级排序）

### 4.1 必须实现（🔴 高优先级）

#### 1. **BarChart** - 柱状图

**理由**：
- 排行榜场景极其常见（热搜、趋势榜、Star 排行）
- 视觉效果直观，易于对比
- 可复用 LineChart 的部分逻辑

**契约设计**：
```python
class BarChartRecord(BaseModel):
    id: str
    x: str  # 类目（如用户名、标题）
    y: float  # 数值（如Star数、热度）
    series: Optional[str] = None  # 分组
    color: Optional[str] = None  # 自定义颜色
    tooltip: Optional[str] = None
```

**典型应用场景**：
- GitHub Trending 按 Star 数排行
- B 站热搜按热度排行
- 虎扑帖子按回复数排行

---

#### 2. **Table** - 表格

**理由**：
- 结构化数据展示不可或缺
- 适合展示多字段、多维度数据
- 后端契约已定义，只需前端实现

**契约已定义**：
```python
class TableViewModel(BaseModel):
    columns: List[TableColumn]
    rows: List[Dict[str, Any]]
```

**典型应用场景**：
- 股票行情数据
- 配置表、参数列表
- 对比多个项目的详细信息

---

### 4.2 建议实现（🟡 中优先级）

#### 3. **PieChart** - 饼图

**理由**：
- 占比、分布场景常见
- 视觉效果好
- 实现复杂度适中

**契约设计**：
```python
class PieChartRecord(BaseModel):
    id: str
    label: str  # 分类名称
    value: float  # 数值
    percentage: Optional[float] = None  # 百分比（可自动计算）
    color: Optional[str] = None
```

**典型应用场景**：
- 编程语言分布
- 内容分类占比
- 来源分布

---

#### 4. **ImageGallery** - 图片画廊

**理由**：
- 图片类内容（壁纸、摄影、设计作品）需要专门展示
- 丰富内容形态
- 提升视觉吸引力

**契约设计**：
```python
class ImageGalleryRecord(BaseModel):
    id: str
    image_url: str  # 图片 URL
    thumbnail_url: Optional[str] = None  # 缩略图
    title: Optional[str] = None
    description: Optional[str] = None
    link: Optional[str] = None  # 点击跳转
```

**典型应用场景**：
- 图片类 RSS 订阅
- 设计作品展示
- 壁纸、摄影作品

---

#### 5. **ProgressBar** - 进度条

**理由**：
- 完成度、占用率等场景常见
- 可配合 StatisticCard 使用
- 实现简单

**契约设计**：
```python
class ProgressBarRecord(BaseModel):
    id: str
    label: str
    value: float  # 当前值
    max_value: float  # 最大值
    percentage: Optional[float] = None  # 百分比（可自动计算）
    status: Optional[str] = None  # success | warning | error
```

**典型应用场景**：
- GitHub Issues 完成度
- 项目进度
- 资源使用率

---

### 4.3 可延后（🟢 低优先级）

- Timeline（时间线）
- TagCloud（标签云）
- RadarChart（雷达图）
- Heatmap（热力图）
- Map（地图）
- Gauge（仪表盘）

---

## 5. 第二阶段：适配器矩阵扩展

### 5.1 高优先级数据源（选择 15-20 个路由）

#### Bilibili（10 个路由）
- `/bilibili/ranking` - 排行榜（BarChart + ListPanel）
- `/bilibili/video` - 视频详情
- `/bilibili/user/bangumi` - 追番列表
- `/bilibili/live/area` - 直播分区
- `/bilibili/topic` - 话题
- `/bilibili/partion/ranking` - 分区排行
- `/bilibili/user/coin` - 投币视频
- `/bilibili/user/fav` - 收藏夹
- `/bilibili/mall/new` - 会员购新品
- `/bilibili/user/channel/video` - 频道视频

#### GitHub（5 个路由）
- `/github/issue` - Issues（Table）
- `/github/pull` - Pull Requests（Table）
- `/github/stars` - Star 历史（LineChart）
- `/github/repos` - 仓库列表
- `/github/file` - 文件历史

#### 微博（3 个路由）
- `/weibo/user` - 用户微博
- `/weibo/search/hot` - 热搜（BarChart）
- `/weibo/keyword` - 关键词

#### 知乎（3 个路由）
- `/zhihu/hotlist` - 热榜（BarChart + ListPanel）
- `/zhihu/people/activities` - 用户动态
- `/zhihu/question` - 问题

#### 其他（5 个路由）
- `/sspai/*` - 少数派（已有 generic 兜底，需专门适配）
- `/douban/movie/playing` - 豆瓣电影
- `/v2ex/topics/hot` - V2EX 热帖
- `/producthunt/today` - Product Hunt
- `/reddit/subreddit` - Reddit 子版块

---

### 5.2 适配器开发策略

#### 规则

1. **按数据源分目录**：
   ```
   services/panel/adapters/
   ├── bilibili/
   │   ├── ranking.py
   │   ├── live.py
   │   └── ...
   ├── github/
   ├── weibo/
   ├── zhihu/
   └── ...
   ```

2. **每个适配器必须包含**：
   - RouteAdapterManifest（声明支持的组件）
   - 完整的契约验证
   - 单元测试
   - 注释说明数据来源和字段映射

3. **组件选择原则**：
   - 优先使用多组件组合（如 ListPanel + BarChart）
   - 避免过度使用 FallbackRichText
   - 根据数据特点选择最佳展示方式

4. **质量要求**：
   - 所有字段必须规范化（去除 HTML、清理文本）
   - 时间字段统一为 ISO8601
   - 数值字段统一为 float
   - 必须通过契约验证

---

## 6. 第三阶段：体验优化与扩展

### 6.1 交互式组件

- Tabs（多标签切换）
- Accordion（折叠面板）
- Carousel（轮播图）
- Filter（筛选器）

### 6.2 富媒体组件

- VideoPlayer（视频播放器）
- AudioPlayer（音频播放器）
- MarkdownView（Markdown 渲染）
- CodeBlock（代码高亮）

### 6.3 性能优化

- 虚拟滚动（长列表优化）
- 懒加载（图片、组件）
- 数据分页
- 缓存优化

---

## 7. 开发流程建议

### 7.1 组件开发流程（契约优先）

1. **契约设计**：
   - 在 `docs/backend-panel-view-models.md` 中添加组件契约
   - 定义 Record 结构、字段说明、推荐 props

2. **后端实现**：
   - 在 `services/panel/view_models.py` 添加 Pydantic 模型
   - 添加 `validate_records` 分支
   - 编写单元测试

3. **前端实现**：
   - 在 `frontend/src/shared/componentManifest.ts` 注册组件
   - 更新 TypeScript 类型（`panelContracts.ts`）
   - 实现 React 组件

4. **验证**：
   - 后端单元测试
   - 前端 Storybook/Mock 测试
   - 端到端测试

---

### 7.2 适配器开发流程

1. **选择路由**：
   - 确认 RSSHub 接口可用
   - 分析数据结构
   - 确定适合的组件

2. **实现适配器**：
   - 创建 `adapters/<datasource>/<name>.py`
   - 定义 Manifest
   - 实现数据转换逻辑
   - 调用 `validate_records` 验证

3. **测试**：
   - 添加样例数据到 `tests/services/test_panel_adapters.py`
   - 测试 manifest、数据转换、stats
   - 测试 `requested_components` 场景

4. **文档**：
   - 在 `docs/adapters/overview.md` 添加说明

---

## 8. 执行建议

### 立即开始

**建议优先级**：
1. 🔴 **BarChart** - 排行榜场景太常见了
2. 🔴 **Table** - 后端契约已定义，只需前端实现
3. 🟡 **PieChart** - 分布场景常见
4. 🟡 **ImageGallery** - 丰富内容形态
5. 🟡 **ProgressBar** - 实现简单，场景明确

**并行开发**：
- 前端：1 人负责组件开发
- 后端：1 人负责适配器开发（可以先用现有组件）
- 协作：每实现 1-2 个新组件，立即编写 3-5 个适配器验证效果

---

## 9. 总结

### 当前状态
✅ **后端核心流程完备**：从用户查询到面板渲染的完整链路已打通
⚠️ **组件严重不足**：只有 4 个基础组件，无法满足多样化展示需求
⚠️ **适配器覆盖不足**：RSSHub 支持数百个路由，当前覆盖率 < 5%

### 核心建议
**可以进入适配器主导开发阶段，但必须先补齐核心组件！**

#### 原因
1. 没有 BarChart，排行榜类数据只能用 ListPanel，体验差
2. 没有 Table，结构化数据无法有效展示
3. 没有 PieChart/ImageGallery，内容形态单一

#### 执行策略
1. **第一阶段（1-2周）**：补齐 5 个核心组件（BarChart、Table、PieChart、ImageGallery、ProgressBar）
2. **第二阶段（2-4周）**：并行开发 20-30 个适配器，利用新组件丰富展示
3. **第三阶段（持续）**：添加交互式组件、富媒体组件、优化体验

**关键成功因素**：
- 契约优先：先定义组件契约，再开发
- 质量优先：每个组件、每个适配器都要有完整测试
- 迭代优先：小步快跑，快速验证效果
- 用户优先：根据实际使用场景调整优先级

---

## 附录：RSSHub 热门路由清单

见 `docs/rsshub-routes-priority.md`（待创建）
